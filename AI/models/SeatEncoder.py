import torch
from torch import nn
import  Date2Vec
class SeatEncoder():
    def __init__(self) -> None:
        self.embedding_parties = nn.Embedding(len(Parties),128)
    def forward():
        pass

